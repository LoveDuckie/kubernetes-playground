apiVersion: v1
kind: Pod
metadata:
  name: test-app-frontend
  labels:
    name: test-app-frontend
spec:
  containers:
  - name: test-app-frontend
    image: <Image>
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: <Port>
